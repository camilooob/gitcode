#include <stdio.h>
int main(void)
{
	float faht, celsius;
	int lower, upper, step;
	lower = 0;
	upper = 300;
	step - 20;

	faht = lower;
	while (faht < upper)
	{
		celsius = (5.0 / 9.0) * (fahr - 32.0)
									printf("4%3.0f%6.1f\n", fahr, celsius);
		fahr = fahr + step;
	}
}
